import React from "react";
import Element from "./Form & Validation/element";

export default class Form extends React.Component {
    render() {
        return (
            <div>
                <Element />
            </div>
        )
    }
}